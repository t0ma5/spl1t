name: Create and publish a Docker image

# Docker image publishing is obsolete for the Cloudflare KV deploy path.
# Kept disabled so accidental tag pushes do not publish a broken Postgres image.

on:
  workflow_dispatch:

jobs:
  skipped:
    runs-on: ubuntu-latest
    steps:
      - name: Docker CD disabled
        run: |
          echo "Use npm run deploy (OpenNext + Cloudflare Workers + KV) instead of Docker."
          exit 0
